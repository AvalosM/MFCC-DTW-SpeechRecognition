#include "io.h"
#include "features.h"

int main(void)
{
    // char *file_in = "data/OSR_us_000_0010_8k.wav";
    // SF_INFO audio_info;
    // float *signal = readwav(file_in, &audio_info);

    // float *signal_features = features(signal, audio_info.frames, audio_info.samplerate);
    
    // char *file_out = "res/OSR_us_features.txt";
    // savesignalf(file_out, signal_features, )
}